import pandas as pd

class Solution:
    def movie_rating(movies: pd.DataFrame, users: pd.DataFrame, movie_rating: pd.DataFrame) -> pd.DataFrame:
        return movie_rating[movie_rating["created_at"] betwe]